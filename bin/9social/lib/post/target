#!/bin/rc

fn usage {
	echo 'usage: 9social/lib/post/target <post-file>' >[1=2]
	exit usage
}

if(! ~ $#* 1)
	usage

9social/lib/post/meta.awk $1 >[2]/dev/null |
	awk '$1=="target" && target=="" { target=$2 }
		END { if(target != "") print target }'
exit $status
